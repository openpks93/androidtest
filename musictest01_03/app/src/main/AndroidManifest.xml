<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.musictest01_03">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MUSIC PLAYER"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BeliverActivity" />
        <activity android:name=".RoundActivity" />
        <activity android:name=".LetitgoActivity" />
        <activity android:name=".DanzaActivity" />
        <activity android:name=".FlowerActivity" />
        <activity android:name=".SharkActivity" />
        <activity android:name=".YearsActivity" />
        <activity android:name=".OneActivity" />
        <activity android:name=".HandsActivity"></activity>
    </application>

</manifest>